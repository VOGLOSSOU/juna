✅ Core Backend:
   ├── Node.js (v18+ LTS)
   ├── Express.js
   ├── TypeScript
   └── Prisma ORM

✅ Base de données:
   ├── PostgreSQL (principal)
   └── Redis (cache + sessions + queue)

✅ Architecture:
   └── Architecture en Couches (Controllers → Services → Repositories)

✅ Sécurité & Auth:
   ├── JWT (Access + Refresh tokens)
   ├── Bcrypt
   ├── Helmet
   ├── CORS
   └── express-rate-limit

✅ Qualité du code:
   ├── Zod (validation)
   ├── Winston (logging)
   ├── Gestion erreurs centralisée
   └── Jest + Supertest (tests)

✅ Jobs Asynchrones:
   └── Bull (queue)

✅ Variables d'environnement:
   └── dotenv

✅ Documentation:
   └── Swagger (je vais te montrer, c'est simple !)

⏳ À intégrer plus tard:
   ├── Paiements (Stripe, Mobile Money)
   ├── Upload fichiers (Multer + Cloudinary)
   └── Géolocalisation (Google Maps API)